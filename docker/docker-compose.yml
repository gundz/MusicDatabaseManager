web:
  image: nginx
  ports:
    - "8080:80"
  volumes:
    - ./html:/code
    - ./default.conf:/etc/nginx/conf.d/default.conf
  links:
    - php

php:
  image: some-content-nginx
  volumes:
    - ./html:/code